version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "🧹 Cleaning cache and old node modules..."
        - rm -rf node_modules package-lock.json
        - echo "📦 Installing dependencies..."
        - npm install
        - echo "🧩 Installing type declarations..."
        - npm install --save-dev @types/bcrypt
        - npm install --save-dev @types/react-google-recaptcha
        - npm install --save-dev @types/jsonwebtoken
        - echo "✅ Type declarations installed"
        - echo "🔧 Running Prisma commands..."
        - echo "🔍 Debug: Checking DATABASE_URL..."
        - echo "DB URL starts with: ${DATABASE_URL:0:30}" || echo "⚠️ DATABASE_URL is empty or not set!"
        - echo "🔍 Debug: DATABASE_URL check complete"
        - npx prisma generate --schema=./prisma/schema.prisma
        - npx prisma db push --accept-data-loss --skip-generate
    build:
      commands:
        - echo "🚀 Building Next.js app..."
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
